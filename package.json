{
  "name": "ahmad-ios-resume",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive résumé (multiple editions) with HTML → PDF export",
  "scripts": {
    "pdf": "node scripts/generate-pdf.js"
  },
  "devDependencies": {
    "puppeteer-core": "^23.0.0"
  }
}
